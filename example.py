from Logger import Logger
import time
import sys
from multiprocessing import Pool


def trainingUpdate(n, loss, dice):


if __name__ == '__main__':
    log = Logger()
    log('hey')
    time.sleep(3)
    log()
